for FILE in TSTs/*; do
	# echo "$FILE"
	VAR=1
	for IN in $FILE/in*; do
		OUT=$FILE/out$VAR
		#echo $FILE/out$VAR
		echo $IN
		./run < $IN > dif && diff $OUT dif
		echo OK!
		let "VAR += 1"
	done
done
