#include <bits/stdc++.h>
using namespace std;

typedef pair <int,int> pii;

vector <pii> adj[100];

void moves (pii st)
{
	if (st.first-2 >= 1 && st.second-1 >= 1)
		adj[st.first].push_back (st.
}

int main ()
{

	return 0;
}
