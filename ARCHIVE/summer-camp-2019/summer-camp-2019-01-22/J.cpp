#include <bits/stdc++.h>
using namespace std;

#define x first
#define y second
typedef vector<int> vi;
typedef pair<int, int> pii;
typedef long long ll;

const int N = 1e5+10;
using pp = pair<ll, int>;

int n, c, d;
int sell[N], buy[N];
vector<pp>

int main()
{
    while (scanf ("%d", &n), n) {
        scanf ("%d %d", &c, &d);
        for (int i = 0; i < n; i++)
            
    }

    return 0;
}
