//Sieve segmented, for when the value n is too large for a single array of bool
//
//The logic is to create an array with sqrt(MAXN) and saving all primes in that "range", in the next instance remove all multiples from that list
#include <bits/stdc++.h>
using namespace std;

int main (){




	return 0;
}
