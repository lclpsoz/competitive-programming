#include <bits/stdc++.h>
using namespace std;

int n, m, con;
int mat[1010][110];
int ans[1010][3];
int bef, now, bef_inc;

int main ()
{
	con = 0;
	while (scanf ("%d", &n), n) {
		scanf ("%d", &m);
		for (int i = 0; i < n; i++)
			for (int j = 0; j < m; j++)
				scanf ("%d", &mat[i][j]);

		for (int i = 0; i < n; i++) {
			for (int j = 0; j < m; j++)
		}


	}

	return 0;
}
