#include <bits/stdc++.h>
using namespace std;

int aux[4];

int main ()
{
	while(scanf("%d.%d.%d-%d", &aux[0], &aux[1], &aux[2], &aux[3])!= EOF){
	}
	for(int i=0; i<4; i++)
		printf("%d ", aux[i]);

	return 0;
}
