#include <bits/stdc++.h>
using namespace std;

const int MAXN = 110;

int i, j;
int qnt, st, obj, qnt_try,
double houses[MAXN][MAXN];

int main(){

	scanf("%d %d %d %d", &qnt, &st, &obj, &qnt_try);

	for (i = 0; i < qnt; i++)
		for (j = 0; j < qnt; j++)
			scanf("%lf", &mat[i][j]);

TR

	return 0;
}
